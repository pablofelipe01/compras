// src/utils/testHelpers.ts
export const documentosPrueba = {
  rut: new File([''], 'rut.pdf', { type: 'application/pdf' }),
  documento: new File([''], 'documento.pdf', { type: 'application/pdf' }),
  certificadoBancario: new File([''], 'certificado.pdf', { type: 'application/pdf' })
};